# Ruff configuration for Python 3.12 - PRODUCTION
# Strict rules for production-grade code

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "S", "C4", "RUF"]
ignore = ["E501"]
